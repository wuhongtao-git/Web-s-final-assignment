const serverPort = 3000
const vuePort = 8000
module.exports = {
  serverPort,
  vuePort,
  vueHost: 'localhost',
  serverPath: `localhost:${serverPort}`,
  vuePath: `localhost:${vuePort}`,
  sqlHost: 'localhost',
  sqlUser: 'root',
  sqlPassword: '121212mysql',
  sqlDatabase: 'database_1'
}
